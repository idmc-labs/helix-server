# Helix Server using graphene-django

```bash
docker-compose up
```

And navigate to `localhost:9000/graphiql/` to view available graphs.

Use `localhost:9000/graphql/` to interact with the server from the client.

